const express = require('express')
const app = express()
const routerGen = require('../index')

const options = {
  docs: { url: '/test/docs', docs: '/test/api-docs.json' },
  swaggerDefinition: {
    info: {
      description: 'project description',
      title: 'test npm',
      version: '1.0.0'
    },
    host: 'localhost:3100',
    basePath: '/test/v1',
    produces: [
      'application/json'
    ],
    schemes: ['http', 'https']
  },
  basedir: __dirname, // app absolute path
  files: ['./src/*.js'] // Path to the API handle folder
}

/* app.use(some middleware) */

routerGen(app, options)

app.listen(3100)
